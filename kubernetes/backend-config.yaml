apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: blog-backend-config
spec:
  timeoutSec: 30
  healthCheck:
    checkIntervalSec: 15
    port: 80
    type: HTTP
    requestPath: /healthz
