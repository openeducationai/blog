apiVersion: v1
kind: Service
metadata:
  name: blog-service
  annotations:
    cloud.google.com/backend-config: '{"default": "blog-backend-config"}'
spec:
  type: NodePort
  selector:
    app: blog
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
